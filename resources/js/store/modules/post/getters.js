export const getters = {
    getPosts(state) {
        return state.posts;
    },

    getPostsMeta(state) {
        return state.meta;
    },

    getPost(state) {
        return state.post;
    }
}
